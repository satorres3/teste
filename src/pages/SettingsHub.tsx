import React, { FC } from 'react';
import Sidebar from '../components/Sidebar';

const SettingsHub: FC = () => {
  return (
    <div>
      <Sidebar />
      <div>Settings Hub Page (Stub)</div>
    </div>
  );
};

export default SettingsHub;
// TODO: replace with settings hub markup
