import React, { FC } from 'react';
import Sidebar from '../components/Sidebar';

const GlobalSettings: FC = () => {
  return (
    <div>
      <Sidebar />
      <div>Global Settings Page (Stub)</div>
    </div>
  );
};

export default GlobalSettings;
// TODO: replace with global settings markup
